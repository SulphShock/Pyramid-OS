# Generated by OpenCode - Pyramid OS TTY autologin fallback
# Law 2: TTY autologin MUST work without SDDM
[Service]
ExecStart=
ExecStart=-/sbin/agetty -o '-p -f -- \\u' --noclear --autologin pyramid %I $TERM
Type=idle
